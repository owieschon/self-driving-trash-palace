{
  "name": "@trash-palace/worker",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/main.ts",
    "dev": "tsx src/main.ts",
    "db:bootstrap": "tsx src/bootstrap-main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@trash-palace/agent": "workspace:*",
    "@trash-palace/application": "workspace:*",
    "@trash-palace/core": "workspace:*",
    "@trash-palace/db": "workspace:*",
    "@trash-palace/observability": "workspace:*",
    "pg-boss": "12.26.0",
    "tsx": "4.23.1"
  }
}
