# Executable examples

This index routes developers to examples that execute repository-owned contracts instead of copying a second implementation.

| Goal                                             | Example                                               |
| ------------------------------------------------ | ----------------------------------------------------- |
| Exercise typed HTTP and MCP parity               | [HTTP and MCP](http-and-mcp.md)                       |
| Run Caretaker against the twelve-case simulation | [Deterministic Caretaker](deterministic-caretaker.md) |

Run `pnpm quest:verify` for the composed credential-free network path. It retains
`evals/reports/credential-free-quest.json` only after its mission and evidence checks pass. The
examples above remain narrower and name their proof boundary.
